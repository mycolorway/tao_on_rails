dist: trusty
sudo: false
language: ruby
cache:
  bundler: true
addons:
  chrome: stable
rvm:
  - 2.4.1
node_js:
  - 'node'
before_install:
  - cd frontend && npm i -g npm
  - cd frontend && npm ci
script:
  - bundle exec rake test
  - cd frontend && npm run ci
