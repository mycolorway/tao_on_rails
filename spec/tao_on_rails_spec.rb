require "spec_helper"

describe TaoOnRails do
  it "has a version number" do
    expect(TaoOnRails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
